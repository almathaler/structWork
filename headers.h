struct nameAge {char *n; int a;};
int printStruct(struct nameAge param);
int modifyStruct(struct nameAge *param, char *name, int age);
struct nameAge example(int i, char *p);
int randAge(int i);
char* randName(int i, char *p);
